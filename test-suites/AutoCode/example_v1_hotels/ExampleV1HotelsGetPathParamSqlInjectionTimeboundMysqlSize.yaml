---
parent: "example_v1_hotels"
name: "ExampleV1HotelsGetPathParamSqlInjectionTimeboundMysqlSize"
description: "The list is paginated. You can provide a page number (default 0) and\
  \ a page size (default 100)"
type: "SUITE"
endpoint: "/example/v1/hotels?size={{@MySQLTimeboundSQLInjections}}"
method: "GET"
auth: "Default"
headers:
- "Accept: application/json"
- "Content-Type: application/json"
testCases: []
assertions:
- "@ResponseTime < 7000"
- "@StatusCode != 404"
tags:
- "V1"
authors:
- "FX Bot"
category: "SQL_Injection"
severity: "Critical"
inactive: false
policies:
  initExec: "Request"
  cleanupExec: "Request"
  logger: "DEBUG"
  timeoutSeconds: 300
  repeatOnFailure: 0
  repeat: 0
  repeatModule: "@MySQLTimeboundSQLInjections"
  repeatDelay: 0
